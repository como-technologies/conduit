# Pinned adroit rev consumed by `just init-adroit`.
# cc43754: published adroit `main` — the previous pin 4416a06 (the v0.2.0 line,
#   post-v0.2.0 hardening, ADR-corpus relocation into docs/src/adr) plus the
#   unsigned disposable-test-repo commits fix and the quinn-proto
#   0.11.14 -> 0.11.15 bump (RUSTSEC-2026-0185) — so the pinned adroit no
#   longer ships a known-advisory dep.
#   Reachable on the adroit remote (main HEAD), so a cold clone resolves it
#   with NO sibling. init-adroit still falls back to a sibling ../adroit when
#   the remote is unreachable (its HEAD, loudly, if it lacks this exact pin).
cc4375499059ee42b8e8fdba43c88c7ee9ab712d
